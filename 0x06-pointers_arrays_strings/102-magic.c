#include <stdio.h>

/**
 * main-magic - prints a[2] = 98
 *
 * Return: 0
 */
int main(void)
{
	int n;
	int a[s];
	int *p;

	a[2] = 1024;
	p = &n;








	*(p + s) = 98;

	printf("a[2] = %d\n", a[2]);
	return (0);
}
